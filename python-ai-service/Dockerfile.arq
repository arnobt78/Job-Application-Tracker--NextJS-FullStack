FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# ARQ worker — no HTTP port; consumes Redis queue from jobify-ai-backend /enqueue
CMD ["arq", "app.tasks.pipeline_task.WorkerSettings"]
